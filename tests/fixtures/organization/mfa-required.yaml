# Organization config for MFA requirement demo
# SCPs at root allow full access, account-level SCP requires MFA for EC2 terminate

scp_hierarchy:
  root:
    - ../scp/full-aws-access.json
  ous: []
  account:
    - ../scp/require-mfa-ec2-terminate.json
